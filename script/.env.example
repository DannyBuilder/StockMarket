# Copy this file to `.env` and fill the values, or set the environment variables
# directly in your system.

# Telegram bot token (from BotFather). Keep this secret and never commit it.
TELEGRAM_BOT_TOKEN= your_bot_token_here

# Your Telegram chat id (the recipient). Can be your user id or a group id.
TELEGRAM_CHAT_ID= your_chat_id_here

# Optional: change default threshold and interval by exporting these vars.
#THRESHOLD=105000
#INTERVAL=100
#LOOP=true
